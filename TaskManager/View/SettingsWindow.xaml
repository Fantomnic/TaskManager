<local:WindowWithBottomButtons x:Class="TaskManager.View.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManager.View"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="450" Width="800" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <CheckBox Grid.ColumnSpan="2" IsChecked="{Binding SetDefaultSectionName}"
                          Checked="SetDefaultSectionNameChecked" Unchecked="SetDefaultSectionNameUnchecked">Заполнять наименование раздела по умолчанию</CheckBox>
                <CheckBox x:Name="incrementSectionName" Grid.Row="1" Grid.ColumnSpan="2" IsChecked="{Binding IncrementSectionName}">Инкрементировать</CheckBox>
                
                <TextBlock Grid.Row="2" Style="{StaticResource fieldDescription}">Наименование нового раздела:</TextBlock>
                <TextBox x:Name="sectionName" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Text="{Binding DefaultSectionName}"></TextBox>
            </Grid>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <CheckBox Grid.ColumnSpan="2" IsChecked="{Binding SetDefaultTaskName}">Заполнять наименование задачи по умолчанию</CheckBox>
                <CheckBox x:Name="incrementTaskName" Grid.Row="1" Grid.ColumnSpan="2" IsChecked="{Binding IncrementTaskName}">Инкрементировать</CheckBox>
                
                <TextBlock Grid.Row="2" Style="{StaticResource fieldDescription}">Наименование новой задачи:</TextBlock>
                <TextBox x:Name="taskName" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Text="{Binding DefaultTaskName}"></TextBox>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <CheckBox>Тест 1</CheckBox>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" FlowDirection="RightToLeft" Orientation="Horizontal">
            <Button Style="{StaticResource serviceButtonWithStandartWidth}" Click="ButtonCancelClick">Отмена</Button>
            <Button Style="{StaticResource serviceButtonWithStandartWidth}" Click="ButtonOKClick">Применить</Button>
        </StackPanel>
    </Grid>
</local:WindowWithBottomButtons>
