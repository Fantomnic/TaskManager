<local:WindowWithBottomButtons x:Class="TaskManager.Views.MessageWindow"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                               xmlns:local="clr-namespace:TaskManager.Views"
                               xmlns:model="clr-namespace:TaskManager.Model"
                               xmlns:res="clr-namespace:TaskManager.Resources"
                               mc:Ignorable="d"
                               WindowStartupLocation="CenterOwner"
                               Style="{StaticResource MessageWindow}"
                               ShowMinMaxButtons="False">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="{Binding Source={x:Static model:Settings.FontSettings},Path=ButtonAreaHeight}"></RowDefinition>
        </Grid.RowDefinitions>

        <Image x:Name="icon" Width="50" VerticalAlignment="Center" Margin="5,5,0,5"></Image>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="{x:Static res:Constants.UniformMarginBase}">
            <TextBlock x:Name="message" Style="{StaticResource BaseStyle}" TextWrapping="Wrap" Margin="0,3,3,3" VerticalAlignment="Center"></TextBlock>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" FlowDirection="RightToLeft" Orientation="Horizontal">
            <Button x:Name="cancelButton" Style="{StaticResource ServiceButton}" Click="ButtonCancelClick" Visibility="Collapsed">Нет</Button>
            <Button x:Name="okButton" Style="{StaticResource ServiceButton}" Click="ButtonOKClick">OK</Button>
        </StackPanel>
    </Grid>
</local:WindowWithBottomButtons>
