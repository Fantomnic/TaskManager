<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:model="clr-namespace:TaskManager.Model">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/TextsResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Порядок расположения важен: если определить шаблон после стиля, в котором он используется, то стиль его не увидит -->
    <ControlTemplate TargetType="Button" x:Key="menuButtonTemplate">
        <Border x:Name="menuButtonBorder"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}"
                Margin="{TemplateBinding Margin}"
                Height="{TemplateBinding Height}"
                Width="{TemplateBinding Width}" >
            <ContentControl HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalAlignment}"
                            Content="{TemplateBinding Content}">
            </ContentControl>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="menuButtonBorder" Property="BorderBrush" Value="{DynamicResource buttonIsMouseOverBorder}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="Button" x:Key="menuButtonIcon">
        <Setter Property="Template" Value="{StaticResource menuButtonTemplate}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource antiMainColor}"></Setter>
        <Setter Property="Background" Value="{x:Null}"></Setter>
        <Setter Property="BorderThickness" Value="2"></Setter>
        <Setter Property="Margin" Value="2"></Setter>
    </Style>

    <Style TargetType="Image" x:Key="menuImageIcon">
        <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        <Setter Property="VerticalAlignment" Value="Center"></Setter>
    </Style>

    <!--TODO: Сделать конвертер для задания пропорциональной высоты кнопки относительно панели-->
    <Style TargetType="Button" x:Key="menuButton">
        <Setter Property="MinHeight" Value="50"></Setter>
        <Setter Property="Width" Value="{Binding ElementName=menu,Path=Width}"></Setter>
        <Setter Property="Background" Value="{Binding ElementName=menu,Path=Background}"></Setter>
        <Setter Property="BorderBrush" Value="{x:Null}"></Setter>
        <Setter Property="FontSize" Value="{Binding Source={x:Static model:Settings.FontSettings},Path=MenuCommandsFont}"></Setter>
        <Setter Property="FontFamily" Value="Monotype Corsiva"></Setter>
    </Style>

    <ControlTemplate TargetType="TabItem" x:Key="tabItemTemplate">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalAlignment}"
                              ContentSource="Header"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource buttonIsMouseOverBackground}"></Setter>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource buttonIsPressedBackground}"></Setter>
                <Setter Property="BorderBrush" Value="{DynamicResource buttonIsMouseOverBorder}"></Setter>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{DynamicResource buttonBackground}"></Setter>
        <Setter Property="BorderThickness" Value="1.5, 1.5, 1.5, 0"></Setter>
        <Setter Property="Template" Value="{StaticResource tabItemTemplate}"></Setter>
    </Style>

    <ControlTemplate TargetType="Button" x:Key="serviceButtonTemplate">
        <Border x:Name="serviceButtonBorder"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="1.5"
                Background="{TemplateBinding Background}"
                Height="{TemplateBinding Height}"
                Width="{TemplateBinding Width}" >
            <ContentControl HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="5,0,5,0"
                            Content="{TemplateBinding Content}">
            </ContentControl>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="serviceButtonBorder" Property="Background" Value="{DynamicResource buttonIsMouseOverBackground}" />
                <Setter TargetName="serviceButtonBorder" Property="BorderBrush" Value="{DynamicResource buttonIsMouseOverBorder}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="true">
                <Setter TargetName="serviceButtonBorder" Property="Background" Value="{DynamicResource buttonIsPressedBackground}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="Button" BasedOn="{StaticResource baseStyle}" x:Key="serviceButtonBase">
        <Setter Property="Template" Value="{StaticResource serviceButtonTemplate}"/>
        <Setter Property="Background" Value="{DynamicResource buttonBackground}"></Setter>
        <Setter Property="Margin" Value="3,2,3,2"></Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource serviceButtonBase}" x:Key="serviceButtonWithStandartWidth">
        <Setter Property="MinWidth" Value="100"></Setter>
        <Setter Property="MaxWidth" Value="200"></Setter>
    </Style>

</ResourceDictionary>